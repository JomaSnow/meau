import 'package:flutter/material.dart';

class AdoptWidget extends StatelessWidget {
  const AdoptWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Adotar");
  }
}
